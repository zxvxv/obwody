blueprint:
  name: "Zarządzanie Obwodami v3 (Deye Default)"
  description: >
    Automatyczny system zarządzania obwodami (Load Shedding).
    Domyślnie skonfigurowany pod falowniki Deye, ale można zmienić sensory.
  domain: automation
  source_url: https://github.com/zxvxv/obwody/blob/main/blueprint.yaml
  input:
    # --- CZUJNIKI ---
    grid_voltage_l1:
      name: "Sensor Napięcia L1"
      # Domyślnie Twoja encja:
      default: sensor.deye_inverter_grid_l1_voltage
      selector:
        entity:
          domain: sensor
          device_class: voltage
    grid_voltage_l2:
      name: "Sensor Napięcia L2"
      # Domyślnie Twoja encja:
      default: sensor.deye_inverter_grid_l2_voltage
      selector:
        entity:
          domain: sensor
          device_class: voltage
    grid_voltage_l3:
      name: "Sensor Napięcia L3"
      # Domyślnie Twoja encja:
      default: sensor.deye_inverter_grid_l3_voltage
      selector:
        entity:
          domain: sensor
          device_class: voltage
    battery_soc:
      name: "Sensor Baterii (SOC)"
      # Domyślnie Twoja encja:
      default: sensor.deye_inverter_battery_bms_soc
      selector:
        entity:
          domain: sensor
          # Usunąłem 'device_class: battery', żeby widział encje Deye bez problemu

    # --- PROGI ---
    voltage_limit:
      name: "Próg Napięcia - AWARIA (V)"
      description: "Poniżej tego napięcia system uzna awarię."
      default: 190
      selector:
        number:
          min: 150
          max: 230
          unit_of_measurement: V
          mode: box
    
    soc_limit_1:
      name: "Próg Baterii - OSTRZEGAWCZY (%)"
      description: "Przy ilu % wyłączyć drugą grupę?"
      default: 80
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "%"
          mode: slider

    soc_limit_2:
      name: "Próg Baterii - KRYTYCZNY (%)"
      description: "Przy ilu % wyłączyć resztę domu?"
      default: 20
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "%"
          mode: slider

    # --- OBWODY ---
    targets_group_1:
      name: "GRUPA 1: Obwody AWARIA SIECI"
      description: "Te urządzenia gasną natychmiast przy zaniku fazy."
      selector:
        target:
          entity:
            domain: switch

    targets_group_2:
      name: "GRUPA 2: Obwody SŁABA BATERIA"
      description: "Te urządzenia gasną przy pierwszym progu SOC."
      selector:
        target:
          entity:
            domain: switch

    targets_group_3:
      name: "GRUPA 3: Obwody PUSTA BATERIA"
      description: "Te urządzenia gasną na końcu."
      selector:
        target:
          entity:
            domain: switch

    # --- POWRÓT ---
    voltage_recovery:
      name: "Próg Napięcia - POWRÓT (V)"
      description: "Powyżej tego napięcia system przywróci zasilanie."
      default: 215
      selector:
        number:
          min: 190
          max: 250
          unit_of_measurement: V
          mode: box

# --- LOGIKA ---
trigger:
  - platform: numeric_state
    entity_id: !input grid_voltage_l1
    below: !input voltage_limit
    id: grid_fail
  - platform: numeric_state
    entity_id: !input grid_voltage_l2
    below: !input voltage_limit
    id: grid_fail
  - platform: numeric_state
    entity_id: !input grid_voltage_l3
    below: !input voltage_limit
    id: grid_fail
    
  - platform: numeric_state
    entity_id: !input battery_soc
    below: !input soc_limit_1
    id: soc_1
    
  - platform: numeric_state
    entity_id: !input battery_soc
    below: !input soc_limit_2
    id: soc_2

  - platform: numeric_state
    entity_id: !input grid_voltage_l1
    above: !input voltage_recovery
    for: "00:00:30"
    id: grid_ok
  - platform: numeric_state
    entity_id: !input grid_voltage_l2
    above: !input voltage_recovery
    for: "00:00:30"
    id: grid_ok
  - platform: numeric_state
    entity_id: !input grid_voltage_l3
    above: !input voltage_recovery
    for: "00:00:30"
    id: grid_ok

action:
  - choose:
      # SCENARIUSZ 1
      - conditions:
          - condition: trigger
            id: grid_fail
        sequence:
          - service: persistent_notification.create
            data:
              title: "⚡ AWARIA"
              message: "Wyłączam Grupę 1."
          - service: switch.turn_off
            target: !input targets_group_1

      # SCENARIUSZ 2
      - conditions:
          - condition: trigger
            id: soc_1
          - condition: or
            conditions:
              - condition: numeric_state
                entity_id: !input grid_voltage_l1
                below: !input voltage_limit
              - condition: numeric_state
                entity_id: !input grid_voltage_l2
                below: !input voltage_limit
              - condition: numeric_state
                entity_id: !input grid_voltage_l3
                below: !input voltage_limit
        sequence:
          - service: persistent_notification.create
            data:
              title: "🔋 BATERIA SŁABA"
              message: "Wyłączam Grupę 2."
          - service: switch.turn_off
            target: !input targets_group_2

      # SCENARIUSZ 3
      - conditions:
          - condition: trigger
            id: soc_2
          - condition: or
            conditions:
              - condition: numeric_state
                entity_id: !input grid_voltage_l1
                below: !input voltage_limit
              - condition: numeric_state
                entity_id: !input grid_voltage_l2
                below: !input voltage_limit
              - condition: numeric_state
                entity_id: !input grid_voltage_l3
                below: !input voltage_limit
        sequence:
          - service: persistent_notification.create
            data:
              title: "🪫 BATERIA KRYTYCZNA"
              message: "Wyłączam Grupę 3."
          - service: switch.turn_off
            target: !input targets_group_3

      # SCENARIUSZ 4
      - conditions:
          - condition: trigger
            id: grid_ok
          - condition: numeric_state
            entity_id: !input grid_voltage_l1
            above: !input voltage_recovery
          - condition: numeric_state
            entity_id: !input grid_voltage_l2
            above: !input voltage_recovery
          - condition: numeric_state
            entity_id: !input grid_voltage_l3
            above: !input voltage_recovery
        sequence:
          - service: persistent_notification.create
            data:
              title: "✅ POWRÓT ZASILANIA"
              message: "Włączam obwody."
          - delay: "00:00:05"
          - service: switch.turn_on
            target: !input targets_group_1
          - delay: "00:00:05"
          - service: switch.turn_on
            target: !input targets_group_2
          - delay: "00:00:05"
          - service: switch.turn_on
            target: !input targets_group_3
mode: restart
